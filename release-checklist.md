/release-checklist skill todo
- version number update must check if tag is correct instead of blindly increasing
- code quality assessment should call skill instead of just scalafix
- code quality skill must be installed
- fix: "According to the release checklist, we should create a release branch from main. However,
you're currently on feature/sprint-9.
- use the gitlab api to merge the MR (if no MR exists, create it)
